__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/
.env

# model weights should never be committed to the repo
models/*.onnx
models/*.bin
models/*.pt
models/weights/

# local speaker memory / meeting data (contains real user data — never commit)
.sahaysar/
*.json.local

demo/*.wav
demo/*.mp4
